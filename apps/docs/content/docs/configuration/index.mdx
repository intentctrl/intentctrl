---
title: Configuration
description: Configure permissions, routing, and data context for your AI assistant
---

IntentCtrl gives you several ways to control how the AI behaves in your application.

## Permissions

Restrict which built-in tools the AI can use — navigate, click, type, and more. Permissions can be set globally on the provider or updated dynamically per user.

→ [Permissions](/docs/configuration/permissions)

## Router Integration

For the `navigate` tool to work with client-side routing (Next.js, React Router), register your router so the AI can move between pages without full reloads.

→ [Router Integration](/docs/configuration/router-integration)
